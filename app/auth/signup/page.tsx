 "use client"; import {useState} from "react"; import {createClient} from "../../../lib/supabase/client"; import {useRouter} from "next/navigation"; export default function Signup(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");const r=useRouter();async function go(e:React.FormEvent){e.preventDefault();const {data,error}=await createClient().auth.signUp({email,password});if(error)setMsg(error.message);else if(data.session)r.push("/dashboard");else setMsg("Check your email if confirmation is enabled.")}return <main className="container"><div className="card" style={{maxWidth:520,margin:"50px auto"}}><h1>Create account</h1><form className="form" onSubmit={go}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><p className="muted">{msg}</p><button className="btn">Create account</button></form></div></main>}