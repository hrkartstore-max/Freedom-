import {createClient} from "../../lib/supabase/server"; import {money} from "../../lib/money"; export default async function Budget(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:i=[]}=await s.from("income_entries").select("amount").eq("user_id",user.id);const {data:e=[]}=await s.from("expense_entries").select("amount").eq("user_id",user.id);const I=i.reduce((a,x)=>a+Number(x.amount),0),E=e.reduce((a,x)=>a+Number(x.amount),0);return <main className="container"><h1>Budget</h1><section className="grid g2"><div className="card">Income<div className="metric">{money(I)}</div></div><div className="card">Expenses<div className="metric">{money(E)}</div></div></section><div className="card" style={{marginTop:18}}>Available after tracked expenses<div className="metric">{money(I-E)}</div></div></main>}