import {createClient} from "../../lib/supabase/server"; import {money} from "../../lib/money"; export default async function Goals(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:g=[]}=await s.from("financial_goals").select("*").eq("user_id",user.id).order("created_at",{ascending:false});return <main className="container"><h1>Goals</h1><div className="grid g2">{g.map(x=><div className="card" key={x.id}><h2>{x.name}</h2><p>{money(x.current_amount)} / {money(x.target_amount)}</p><p className="muted">{Math.round(Number(x.current_amount)/Number(x.target_amount)*100)}% complete</p></div>)}</div></main>}