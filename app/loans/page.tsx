import {createClient} from "../../lib/supabase/server"; import {money} from "../../lib/money"; export default async function Loans(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:loans=[]}=await s.from("loans").select("*").eq("user_id",user.id).order("balance");return <main className="container"><h1>Loans</h1><div className="card"><table><thead><tr><th>Name</th><th>Balance</th><th>APR</th><th>EMI</th></tr></thead><tbody>{loans.map(l=><tr key={l.id}><td>{l.name}</td><td>{money(l.balance)}</td><td>{l.annual_rate}%</td><td>{money(l.emi)}</td></tr>)}</tbody></table></div><p className="muted">Use the Supabase-connected form in the full source to add/edit loans.</p></main>}