import {createClient} from "../../lib/supabase/server"; import {money} from "../../lib/money"; export default async function Payments(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:p=[]}=await s.from("loan_payments").select("*").eq("user_id",user.id).order("payment_date",{ascending:false});return <main className="container"><h1>Payments</h1><div className="card"><table><thead><tr><th>Date</th><th>Amount</th><th>Interest</th><th>Principal</th></tr></thead><tbody>{p.map(x=><tr key={x.id}><td>{x.payment_date}</td><td>{money(x.amount)}</td><td>{money(x.interest)}</td><td>{money(x.principal)}</td></tr>)}</tbody></table></div></main>}