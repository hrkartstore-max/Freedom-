export type Loan={id:string;name:string;balance:number;annual_rate:number;emi:number;status:string}; export const active=(l:Loan[])=>l.filter(x=>x.status!=="CLOSED"&&Number(x.balance)>0); export const snowball=(l:Loan[])=>[...active(l)].sort((a,b)=>Number(a.balance)-Number(b.balance)); export const avalanche=(l:Loan[])=>[...active(l)].sort((a,b)=>Number(b.annual_rate)-Number(a.annual_rate)); export const total=(l:Loan[])=>l.reduce((s,x)=>s+Number(x.balance||0),0); export const emi=(l:Loan[])=>active(l).reduce((s,x)=>s+Number(x.emi||0),0);